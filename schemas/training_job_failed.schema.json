{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://tg-news.local/schemas/training_job_failed.schema.json",
  "title": "Training Job Failed",
  "type": "object",
  "required": ["event_id", "event_type", "event_timestamp", "event_version", "source_system", "payload"],
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "training_job_failed" },
    "event_timestamp": { "type": "string", "format": "date-time" },
    "event_version": { "type": "string" },
    "source_system": { "type": "string" },
    "trace_id": { "type": "string" },
    "payload": {
      "type": "object",
      "required": ["training_job_id", "status", "error_message"],
      "properties": {
        "training_job_id": { "type": "string" },
        "status": { "const": "failed" },
        "error_message": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
