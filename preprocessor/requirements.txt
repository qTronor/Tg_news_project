aiokafka>=0.10.0
asyncpg>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.1
PyYAML>=6.0.1
jsonschema>=4.21.1
aiohttp>=3.9.0
prometheus-client>=0.20.0
# Language detection. fasttext-wheel ships pre-built wheels so Dockerfile
# does not need a C toolchain. Pinned upper-bound to avoid ABI surprises.
fasttext-wheel>=0.9.2,<0.10
numpy>=1.24.0
